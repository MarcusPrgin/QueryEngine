# Core — no external dependencies required for basic functionality
# Everything below is optional or dev tooling

# Optional: Parquet support (highly recommended — shows columnar vs row storage tradeoff)
pyarrow>=15.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: faster CSV parsing (drop-in speedup, no code changes needed)
# python-csvkit

# Optional: for the WASM/CLI demo
# typer[all]>=0.12.0
